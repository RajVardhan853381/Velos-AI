# Leave VITE_API_BASE unset in dev — Vite proxy handles /api and /ws routing to localhost:8000
# Set this in production to point to your deployed backend, e.g.:
# VITE_API_BASE=https://your-backend.onrender.com
